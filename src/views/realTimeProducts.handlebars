<h2>Add New Product</h2>
<div id="product-form">
    <div>
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" required>
    </div>
    <div>
        <label for="description">Description:</label>
        <textarea id="description" name="description" required></textarea>
    </div>
    <div>
        <label for="code">Code:</label>
        <input type="text" id="code" name="code" required>
    </div>
    <div>
        <label for="price">Price:</label>
        <input type="number" id="price" name="price" step="0.01" required>
    </div>
    <div>
        <label for="status">Status (Available):</label>
        <input type="checkbox" id="status" name="status">
    </div>
    <div>
        <label for="stock">Stock:</label>
        <input type="number" id="stock" name="stock" required>
    </div>
    <div>
        <label for="category">Category:</label>
        <input type="text" id="category" name="category" required>
    </div>
    <button id="submit-product">Add Product</button>
</div>

<h1>Product List</h1>
<div id="products-div">
    {{#if products.length}}
    {{#each products}}
        <div>
          <h3>{{this.title}}</h3>
          <p>Code: {{this.code}}</p>
          <p>Description: {{this.description}}</p>
          <p>Price: ${{this.price}}</p>
          <p>Status:
            {{#if this.status}}Available{{else}}Not available{{/if}}</p>
          <p>Stock: {{this.stock}}</p>
          <p>Category: {{this.category}}</p>
        </div>
      {{/each}}
    {{/if}}
</div>
<link rel="stylesheet" href="/css/stylesProductsRealTime.css">
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="/socket.io/socket.io.js"></script> 
<script src="/js/productsRealTime.js"></script>